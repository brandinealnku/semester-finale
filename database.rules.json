{
  "rules": {
    "sessions": {
      "$sessionId": {
        ".read": "auth != null",
        "meta": {
          "hostUid": {
            ".write": "auth != null && (!data.exists() || data.val() === auth.uid)",
            ".validate": "newData.isString() && newData.val() === auth.uid"
          }
        },
        "state": {
          ".write": "auth != null && root.child('sessions/' + $sessionId + '/meta/hostUid').val() === auth.uid"
        },
        "participants": {
          "$participantId": {
            ".write": "auth != null && (root.child('sessions/' + $sessionId + '/meta/hostUid').val() === auth.uid || ((!data.exists() && newData.child('authUid').val() === auth.uid) || (data.exists() && data.child('authUid').val() === auth.uid && newData.child('authUid').val() === auth.uid)))",
            ".validate": "newData.hasChildren(['id','name','joined','stage','lastSeen','deviceType','authUid'])"
          }
        }
      }
    }
  }
}
